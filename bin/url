#!/bin/bash

DIR="$(dirname "$0")"
. "${DIR}/config"

echo $(gcloud beta run services describe $SERVICE_NAME \
    --platform gke \
	--cluster $CLUSTER_NAME \
	--cluster-location $CLUSTER_ZONE \
	--namespace $SERVICE_NAMESPACE \
    --format="value(status.domain)")